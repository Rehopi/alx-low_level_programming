#include "main.h"

/**
 * _isdigit - A function that checks for a digit.
 * @c: input
 * Return: 1 if c is a digit, 0 otherwise
 */
int _isdigit(int c)
{
int a = isdigit(c);

if (a == 0)
return (a);
else
return (1);
}
